⚠️ This is currently a WIP build and not ready for production. ⚠️

### Installation
```npm i```

### Dev mode:
```npm run dev```

### Watch mode (hot reload with browser-sync):
Note: update package.json watch command with the url of your local site.

```npm run watch```

